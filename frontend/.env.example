# Frontend env example. Copy to .env for production builds.
# In dev, Vite proxies /api to localhost:5000, so this can stay empty.
# In production set it to your deployed backend, e.g. https://taskflow-api.onrender.com
VITE_API_URL=
